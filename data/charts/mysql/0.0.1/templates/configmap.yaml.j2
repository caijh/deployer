---
apiVersion: v1
kind: ConfigMap
metadata:
  name: "{{ name }}"
data:
  custom.cnf: |
    [mysqld]
    default_storage_engine=innodb
    max_connections=2000
    skip_external_locking
    lower_case_table_names=1
    skip_host_cache
    skip_name_resolve
